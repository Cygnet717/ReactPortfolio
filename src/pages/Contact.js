import React, { useState, useEffect } from 'react';

const Contacts = () => {
  const [ ,  ] = useState([]);

  useEffect(() => {
    
  }, []);

  return (
    <div className="">
      <h1>Contacts</h1>
    </div>
  );
};

export default Contacts;